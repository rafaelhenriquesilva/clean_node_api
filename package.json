{
  "name": "clean_node_api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "jest --silent --colors --coverage --noStackTrace --passWithNoTests",
    "test:unit": "npm test -- --watch -c jest-unit-config.js",
    "test:integration": "npm test -- --watch -c jest-integration-config.js",
    "test:ci": "npm test --"
  },
  "lint-staged": {
    "*.js": [
      "standard --fix",
      "git add ."
    ]
  },
  "standard": {
    "env": [
      "jest"
    ]
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "jest": "^29.7.0",
    "lint-staged": "^15.0.2",
    "standard": "^17.1.0"
  },
  "dependencies": {
    "bcrypt": "^5.1.1",
    "validator": "^13.11.0"
  }
}
